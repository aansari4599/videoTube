import { Router } from "express";

const router = Router();

// video like, tweet like, comment like, getLikedVideos -> all path should be secured (ids will be passed for each to reference)

export default router;
