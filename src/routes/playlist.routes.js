import { Router } from "express";

const router = Router();

// addVideoto playlist , remove video from playlist, create playlist, delete playlist, getPlaylist by id, getplaylistby user, update playlist

export default router;
